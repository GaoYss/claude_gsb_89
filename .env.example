# docker compose 使用的环境变量，复制为 .env 后按需修改
# 数据库
POSTGRES_USER=reservoir
POSTGRES_PASSWORD=reservoir
POSTGRES_DB=reservoir

# 端口映射
WEB_PORT=8080
BACKEND_PORT=8000

# 首次启动是否灌入演示数据（水库台账非空时不会重复灌入）
SEED_DEMO_DATA=true

# 允许的跨域来源，* 表示放开（前端容器已通过 nginx 同源代理，一般无需修改）
CORS_ORIGINS=*

TZ=Asia/Shanghai

